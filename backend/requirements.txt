# ==========================================================
# 企业级 RAG 知识库问答系统 - Python 依赖
# 毕设项目：LangChain + FastAPI + Qdrant
# ==========================================================

# ---------- Web 框架 ----------
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9

# ---------- 配置 & 数据校验 ----------
pydantic>=2.9
pydantic-settings>=2.6

# ---------- 数据库（异步）----------
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
asyncpg>=0.30
alembic>=1.14

# ---------- 认证 ----------
PyJWT>=2.10
bcrypt>=4.2

# ---------- LangChain RAG ----------
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.2
langchain-community>=0.3
langchain-text-splitters>=0.3
langgraph>=0.2

# ---------- Agent 工具（计算/天气/日历）----------
httpx>=0.27
zhdate>=0.1     # 农历日期转换（PyPI 最高版本 0.1）

# ---------- 向量数据库（与服务端 qdrant v1.19 对齐，query_points API）----------
qdrant-client>=1.19,<2.0

# ---------- 文档解析（知识库上传）----------
pypdf>=5.1
python-docx>=1.1
openpyxl>=3.1

# ---------- 本地向量模型（bge-small-zh，ONNX Runtime CPU 推理）----------
# 1.6G 小机装不下 torch（导入 ~500MB 必换页打爆），改 ONNX Runtime（~80MB）。
# ONNX 权重由 scripts/export_onnx.py 在开发机导出，随部署上传。
onnxruntime>=1.18
tokenizers>=0.15

# ---------- 混合检索（jieba 中文分词 + BM25）----------
jieba>=0.42
rank-bm25>=0.2.2

# ---------- 语义缓存（Redis 存储，未配置则内存实现）----------
redis>=5.2

# ---------- 接口限流 ----------
slowapi>=0.1.9

# ---------- MCP（Model Context Protocol）----------
# 把知识库问答能力暴露为 MCP 工具（Claude Code / Claude Desktop / Cursor 可直连）
mcp>=2.0,<3
